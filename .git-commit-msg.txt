feat: implement multi-tenancy phases 1-8 (M:N membership model)

Phase 1-2: Tenant model with Membership join table (RBAC)
Phase 3: Backend API endpoints (tenant CRUD, membership, invitations)
Phase 4: Data pipeline updates (hexagonal tenant isolation)
Phase 5: GCS bucket per-tenant isolation
Phase 6: Auth flow updates (auto-tenant on register/login)
Phase 7: Frontend (TenantContext, WorkspaceSwitcher, team page)
Phase 8: Data migration (48 memberships, schema/domain/slug updates)

Key changes:
- auto_create_schema=False (shared-schema FK filtering)
- Membership model with Role enum and invitation flow
- TenantPermission classes for RBAC enforcement
- X-Tenant-ID header injection in frontend API client
- migrate_to_membership.py (idempotent, dry-run/apply)
