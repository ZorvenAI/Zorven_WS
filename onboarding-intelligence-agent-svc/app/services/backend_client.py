"""Django REST client — Company PATCH, provenance writes.

Design §16 · implemented by story B-01.

Scaffolded by A-05. The body raises NotImplementedError deliberately: a
stub that silently returns None would let a later story ship a no-op
that passes its tests.
"""

from __future__ import annotations

_NOT_YET = (
    "Django REST client — Company PATCH, provenance writes. — implemented by B-01"
)


class BackendClient:
    """Not yet implemented."""

    def __init__(self, *args: object, **kwargs: object) -> None:
        raise NotImplementedError(_NOT_YET)
